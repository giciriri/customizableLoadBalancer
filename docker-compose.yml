version: '3.8'

services:
  flask-server1:
    build:
      context: .  # Directory containing your Dockerfile
      dockerfile: app.Dockerfile  # Specify the Dockerfile name
    environment:
      - SERVER_ID=Server1  # Updated to match your server ID convention
    ports:
      - "5001:5000"  # Maps container port 5000 to host port 5001

  flask-server2:
    build:
      context: .  # Directory containing your Dockerfile
      dockerfile: app.Dockerfile  # Specify the Dockerfile name
    environment:
      - SERVER_ID=Server2  # Updated to match your server ID convention
    ports:
      - "5002:5000"  # Maps container port 5000 to host port 5002

  flask-server3:
    build:
      context: .  # Directory containing your Dockerfile
      dockerfile: app.Dockerfile  # Specify the Dockerfile name
    environment:
      - SERVER_ID=Server3  # Updated to match your server ID convention
    ports:
      - "5003:5000"  # Maps container port 5000 to host port 5003

  flask-server4:
    build:
      context: .  # Directory containing your Dockerfile
      dockerfile: app.Dockerfile  # Specify the Dockerfile name
    environment:
      - SERVER_ID=Server4  # Updated to match your server ID convention
    ports:
      - "5004:5000"  # Maps container port 5000 to host port 5004

  load-balancer:
    build:
      context: .  # Directory containing your Dockerfile
      dockerfile: load_balancer.Dockerfile  # Specify the Dockerfile name
    ports:
      - "4001:4001"  # Maps container port 4001 to host port 4001
